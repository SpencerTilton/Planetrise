interface SelectableObject {

    // When an object is selected, this function will be called
    // Allows for different hotkeys with different selections
    Controlls();            // MUST CONTAIN UPDATE FUNCTION
}